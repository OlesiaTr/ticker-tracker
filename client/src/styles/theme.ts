const colors = {
  white: '#FFFFFF',
  mainBlack: '#0C0B0B',
  successGreenText: '#137333',
  successGreenBg: '#e6f4ea',
  lossRedText: '#a50e0e',
  lossRedBg: '#fce8e6',
  mainGray: '#F5F5F7',
  borderGray: 'rgba(12, 11, 11, 0.1)',
  mainPink: '#FF868E',
};

const fonts = {
  main: "'Roboto', sans-serif",
  heading: "'Baloo Bhaijaan 2', sans-serif",
};

const fontWeights = {
  medium: 500,
  bold: 700,
};

const fontSizes = {
  m: '16px',
  xm: '18px',
  l: '24px',
  xl: '28px',
};

const borders = {
  medium: '2px solid',
  bold: '4px solid',
};

const shadows = {
  card: '0px 4px 4px rgba(135, 135, 135, 0.2)',
};

const transition = {
  main: 'all 250ms cubic-bezier(0.4, 0, 0.2, 1)',
};

const gradient = {
  linearGradient: 'linear-gradient(295deg, #ff7e7e, #ff4848)',
};

export const theme = {
  colors,
  fonts,
  fontWeights,
  fontSizes,
  borders,
  shadows,
  transition,
  gradient,
};
